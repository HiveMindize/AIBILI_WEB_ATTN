Introdu��o de per�odo pretendido:
	-Inserir data de in�cio e fim
	Solu��o mais simples mas menos escal�vel caso se pretendam per�odos disjuntos
	de f�rias. Solu��o: bot�o que adiciona mais campos para per�odos adicionais.
	Continua a ser imposs�vel gerir horas espec�ficas para cada dia (apenas para um per�odo).

	-Calend�rio interativo
	Mais trabalhoso de implementar mas muito mais escal�vel. Permite escolher v�rios per�odos
	de f�rias sem recorrer a v�rios campos de formul�rio. 
	Permite flexibilidade ao gerir v�rios per�odos.
	Torna vi�vel gest�o vari�vel de tempo para cada dia (por exemplo, meio dia de f�rias em vez de um dia inteiro).
	Tamb�m complicado de persistir. Possivelmente existem j� solu��es, investigar.

Prefer�ncia por calend�rio interativo pela flexibidade acrescida. Investigar poss�veis solu��es j� implementadas.

Persist�ncia do calend�rio interativo:
	-Continuar a usar Excel e guardar informa��o em formato XML ou XHTML.
	 Conte�do Excel � f�cil de  persistir via XML atrav�s de PHP.
	 Pouco escal�vel: pode n�o ser poss�vel integrar na intranet diretamente, obrigando a importar/exportar um ficheiro.

	-Integrar conte�do na p�gina com script que guarde em XML.
	 Persist�vel via XML/XHTML. Escal�vel, mas complicado de implementar sem solu��o externa.Investigar.

	Em todas as solu��es � necess�rio recolher todas as entradas relativas a um utilizador e apresentar no mesmo mapa trimestral.
	Impedir preencher c�lulas de fins de semana e feriados: procurar servi�o de calend�rio que resolva este problema.
	Preencher mapa de marca��o de f�rias com todos os colaboradores.

	Guardar per�odos sempre com in�cio e fim, apenas de mostra o per�odo graficamente ao utilizador (JavaScript?)

Base de dados:

employees(name)

projects(name)

admins(name)

departments(name, director)
director: FK(emloyee.nome) 

supervises_project(supervisor, project)
supervisor: FK(employee.name)
project: FK(project.name)

supervises_employee(supervisor, employee)
supervisor: FK(employee.name)
employee: FK(employee.name)

collaborates(employee, project)
employee: FK(employee.name)
project: FK(project.name)

requests(employee, type, begin, end)
employee: FK(employee.name)

holiday_map(employee, begin, end)
employee: FK(employee.name)


Chefia direta:
-Supervisor-colaborador. Um colaborador pode ter v�rios supervisores, em projetos/atividades diferentes. Muitos para muitos. 
Associ�vel pelas rela��es supervisiona_projeto e colabora. Confirmar se poss�vel.

Dire��o de unidade:
Diretor-chefe. Uma unidade tem um diretor, associar aos chefes que trabalham nessa �rea. Um para muitos.

